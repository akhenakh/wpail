version: "3"

vars:
  BIN: bin/wpail
  LDFLAGS: -s -w
  # macOS uses a cgo backend (libproc); Linux builds stay static.
  GOOS:
    sh: go env GOOS
  CGO_ENABLED:
    sh: |
      case "$(go env GOOS)" in darwin) echo 1 ;; *) echo 0 ;; esac

tasks:
  build:
    desc: Build a stripped binary into bin/
    cmds:
      - CGO_ENABLED={{.CGO_ENABLED}} go build -trimpath -ldflags "{{.LDFLAGS}}" -o {{.BIN}} .
    sources:
      - "*.go"
      - listen/*.go
      - tui/*.go
      - go.mod
      - go.sum
    generates:
      - "{{.BIN}}"

  test:
    desc: Run all tests
    cmds:
      - go test ./...

  vet:
    desc: Run go vet
    cmds:
      - go vet ./...

  fix:
    desc: Apply go fix modernizations
    cmds:
      - go fix ./...

  fmt:
    desc: Format all Go files
    cmds:
      - gofmt -l -w .

  check:
    desc: vet + fix-check + test + build (pre-commit gate)
    deps: [vet]
    cmds:
      - go fix ./...
      - task: test
      - task: build

  tidy:
    desc: Reconcile go.mod/go.sum
    cmds:
      - go mod tidy

  run:
    desc: Run against this machine — pass args after -- e.g. `task run -- 6666`
    cmds:
      - go run . {{.CLI_ARGS}}

  install-bin:
    desc: Install into $GOPATH/bin
    cmds:
      - CGO_ENABLED={{.CGO_ENABLED}} go install -trimpath .

  clean:
    desc: Remove build artifacts
    cmds:
      - rm -rf bin
